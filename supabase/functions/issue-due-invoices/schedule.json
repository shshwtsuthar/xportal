{
  "name": "issue-due-invoices-hourly",
  "description": "Hourly issue/send invoices whose issue_date is due",
  "cron": "0 * * * *",
  "request": {
    "method": "POST",
    "headers": {
      "Authorization": "Bearer ${SUPABASE_SERVICE_ROLE_KEY}"
    },
    "body": "{}"
  }
}
